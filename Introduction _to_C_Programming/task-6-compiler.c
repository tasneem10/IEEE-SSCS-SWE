
#include <stdio.h>

int main () {

    printf("Hello world!\n");

    int x=5;

    if (5==0){

        solve_tasks_without_effort ();

    }

    return(0);
}
//implicit declaration of function ‘solve_tasks_without_effort'
// it gives an error due to debugging the whole code then give the output instead of python compile line by line 